// need to be created after choosing the project and creating thr databases
const router = require('express').Router();

// Import the custom middleware
const withAuth = require('../utils/auth');










module.exports = router;